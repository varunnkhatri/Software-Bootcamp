// const prices = [100, 200, 300, 400];

// const gst = prices.map(function(prices) {
//     return prices + 18/100;
// });

// console.log(gst);


const employees = [
    { name: "Varun", salary: 60000 },
    { name: "Rahul", salary: 45000 },
    { name: "Amit", salary: 75000 }
];

const names = employees.map(function(employee){
return employee.name;
})

console.log(names);
